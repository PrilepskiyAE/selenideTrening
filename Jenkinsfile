pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                echo 'Hello, Jenkins is working!'
                sh 'pwd'
                sh 'ls -la'
            }
        }
    }
}
